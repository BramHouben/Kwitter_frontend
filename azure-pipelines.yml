# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

pool:
 vmImage: 'ubuntu-20.04'

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Npm@1
      inputs:
        command: 'install'
    - task: SonarCloudPrepare@1
      inputs:
        SonarCloud: 'SonarFrontend'
        organization: '410994'
        scannerMode: 'CLI'
        configMode: 'manual'
        cliProjectKey: '410994_ReactKwitter'
        cliProjectName: 'ReactKwitter'
        cliSources: '.'
    - task: Npm@1
      inputs:
       command: 'custom'
       customCommand: 'run build'

    - task: SonarCloudAnalyze@1
      
    - task: SonarCloudPublish@1
      inputs:
        pollingTimeoutSec: '300'

    # - task: Docker@2
    #   displayName: Build an image
    #   inputs:
    #     command: build
    #     dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
    #     arguments: '-t 22021999/reactkwitter:latest'
          
          
    # - task: Docker@2
    #   inputs:
    #     containerRegistry: 'DockerPersonalProject'
    #     repository: '22021999/reactkwitter'
    #     command: 'push'
    #     tags: 'latest'

    # - task: AzureRmWebAppDeployment@4
    #   inputs:
    #       ConnectionType: 'AzureRM'
    #       azureSubscription: 'Azure for Students (b6bb145a-121c-46c2-8b30-f8d25c3b1fee)'
    #       appType: 'webAppContainer'
    #       WebAppName: 'dockerReactKwitter'
    #       DockerNamespace: '22021999'
    #       DockerRepository: 'reactkwitter'
    #       DockerImageTag: 'latest'
      